export class Bomb {
  constructor(x, y, flameRange) {
    this.x = Math.round(x);
    this.y = Math.round(y);
    this.flameRange = flameRange;
  }

  explotion(map) {
    
  }
}

export default Bomb;
