import {
  useState,
  Create,
  Div,
  Li,
  Span,
  Input,
  Button,
} from "../../mostJS/index.js";
import { SOCKET_TYPES } from "../utils.js";

const Chat = ({ ws }) => {
  return Div({}, "hello world");
};
